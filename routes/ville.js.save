var express = require('express');
var router = express.Router();

/* GET Ville page */
router.get('/ville', function(req, res, next) {
  res.render('ville', { title: 'Ville' });
});


module.exports = router;
